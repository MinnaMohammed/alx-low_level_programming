#include <stdio.h>
/**
 *sub - a function that subtracts two integers.
 *@a: input number
 *@b: input number
 * Return: the result of subtracting 2 numbers.
*/
int sub(int a, int b)
{
	return (a - b);
}
