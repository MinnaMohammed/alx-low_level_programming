#include <stdio.h>
/**
 * mul - a function that multiplies two integers.
 *@a: input number
 *@b: input number
 * Return: the result of multiplying 2 numbers.
*/
int mul(int a, int b)
{
return (a * b);
}
